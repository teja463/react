import "../globals.css";

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <header>Header Route group Layout</header>
        <main>{children}</main>
        <footer>Footer</footer>
      </body>
    </html>
  );
}
